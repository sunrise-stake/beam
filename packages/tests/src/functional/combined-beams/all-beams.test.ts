describe("All beams", () => {
  before(async () => {
    // set up core
    // set up marinade stake pool, spl stake pool, marinade lp beams
  });

  context("Deposit into marinade stake pool", () => {
    console.log("TODO");
  });
});
