/// Seed of the PDA that can authorize spending from the vault that holds pool tokens.
pub const VAULT_AUTHORITY: &[u8] = b"vault_authority";
/// Seed of this program's state address.
pub const STATE: &[u8] = b"sunrise_spl";

pub const EXTRACT_YIELD_STAKE_ACCOUNT: &[u8] = b"extract_yield_stake_account";
